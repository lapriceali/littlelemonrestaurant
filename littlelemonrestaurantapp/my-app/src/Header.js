import React from 'react';

const Header = () => {
    return (
        <header>
           <img src={logo.svg} alt="Little Lemon Logo" />
        </header>
    );
};

export default Header;
